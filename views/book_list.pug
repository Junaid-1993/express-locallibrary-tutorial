extends layout

block content
  h1= title

  ul
    each book in book_list
      li
        //- book.url is 'virtual property'
        a(href=book.url) #{book.title}
        |  (#{book.author.name})

    else
      li There are no books.
